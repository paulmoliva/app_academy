<h1><%=@goal.title  %></h1>
<h2>A goal by <%= User.find(@goal.user_id).username %></h2>

<p>
  <%= @goal.body %>
</p>

<%= link_to "All Goals", goals_url %>
<% if current_user.id == @goal.user_id %>
  <%= link_to "Edit Goal", edit_goal_url(@goal) %>
  <%= button_to "Delete Goal", goal_url(@goal), method: :delete %>
<% end %>
