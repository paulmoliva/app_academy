<% action = (band.persisted? ? band_url(band) : bands_url) %>


<form action= "<%= action %>" method="post">
  <%= auth_token %>
  <%= form_method(method) %>
  <label>Band Name
    <input type="text" name="band[name]" value="<%= band.name %>">
  </label>
  <input type="submit" value="<%= edit_or_create(method)%> Band">
</form>
